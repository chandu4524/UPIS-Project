# GPIP Frontend — copy to .env for local overrides
# Leave unset to use Vite dev proxy (/api -> http://127.0.0.1:8000)

# Local dev (default via vite.config.js proxy):
# VITE_API_BASE_URL=/api

# Direct backend URL (e.g. production build):
# VITE_API_BASE_URL=http://127.0.0.1:8000/api
